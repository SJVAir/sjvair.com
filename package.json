{
  "name": "sjvair",
  "version": "1.0.1",
  "private": true,
  "engines": {
    "node": "16.13.0"
  },
  "scripts": {
    "heroku-postbuild": "invoke build && python manage.py sync_staticfiles_s3 && rm -rf public/static"
  },
  "dependencies": {
    "@sjvair/monitor-map": "^1.32.0",
    "bulma": "^0.9.4"
  }
}
